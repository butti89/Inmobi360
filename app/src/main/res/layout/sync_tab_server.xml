<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="lens.inmo360.MainActivity">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/server_property_base_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingLeft="5dp"
        android:paddingRight="5dp"
        android:layout_marginTop="5dp"
        android:scrollbars="vertical"
        tools:listitem="@layout/base_property_row"/>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/download_floating_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scaleType="center"
        app:backgroundTint="@color/primary"
        android:src="@drawable/ic_file_download_24dp"
        android:tint="#ffffff"
        android:layout_margin="@dimen/fab_margin"
        app:layout_anchor="@id/server_property_base_recycler_view"
        app:layout_anchorGravity="bottom|right|end"
        />

</android.support.design.widget.CoordinatorLayout>